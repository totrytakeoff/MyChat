The header is clean and well-designed: `FanoutPolicy` abstract interface, `AllSessionsFanoutPolicy` default, `PushService` with `push_to_user` and `set_fanout_policy`. Let me read the implementation and the other changed files.
